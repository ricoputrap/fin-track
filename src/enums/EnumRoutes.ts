enum EnumRoutes {
  LOGIN = "/login",
  SIGNUP = "/signup",
  DASHBOARD = "/dashboard",
  TRANSACTIONS = "/transactions",
  BUDGETS = "/budgets",
  CATEGORIES = "/categories",
  WALLETS = "/wallets",
  GOALS = "/goals",
}

export default EnumRoutes